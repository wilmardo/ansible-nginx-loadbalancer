---
- hosts: all
  become: yes
  vars:
    proxies:
      couchpotato:
        domain: "example.com"
        ip: "192.168.1.10"
        port: 5050
        basic_auth:
          name: user
          password: password

  roles:
    - role: geerlingguy.nginx
    - role: ansible-role-nginx-loadbalancer